<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Encounterly - A D&D Encounter Builder</title>
    <link rel="stylesheet" href="/css/main.css" />
</head>

<body class="background-img" style="--image-url:url(https://i.imgur.com/3d3mxdk.png)">
    <%- include('../partials/header.ejs') %>
        <div class="center">
            <div class="player">
                <div class="player-items">
                    <h1>Monsters</h1>
                    <a href="/monsters/new">Add a Monster</a>
                    <% if (monsters.length) { %>
                        <% monsters.forEach(monster=> { %>
                            <a href="/monsters/<%= monster._id %>">
                                <%= monster.name %>
                            </a>
                            <% }) %>
                                <% } else { %>
                                    <p>There are no monsters in this list</p>
                                    <% } %>
                </div>
            </div>
        </div>
    <%- include('../partials/footer.ejs') %>
</body>
</html>